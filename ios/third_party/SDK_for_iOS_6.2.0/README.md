# SDK_for_iOS_6.2.0（配置用）

ここに、ベンダー配布の **SDK_for_iOS_6.2.0** フォルダの中身を配置してください。

`tools/setup_ios_sdk.ps1` で共有フォルダからコピーするか、手動でコピーしたうえで Git に追加します。

配置後の例（ベンダー構成により異なります）:

- `*.xcframework` / `*.framework`
- ヘッダ・サンプルプロジェクト等

Flutter ネイティブ連携（Method Channel 等）は、アプリ側の Swift/Objective-C 実装と合わせて追加します。
